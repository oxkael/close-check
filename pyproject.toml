[project]
name = "closewatch"
version = "0.1.0"
description = "CloseWatch — market closure gap signal dashboard"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
